# Core dependencies for xpai-camera-control
# Install: pip install -r requirements.txt

# MCP Server
mcp>=1.0.0, <2.0.0

# Camera control
opencv-python>=4.8.0
onvif-zeep>=0.2.12
requests>=2.28.0
psutil>=5.9.0

# ffmpeg binaries (for toggle_recording / ffprobe stream detection)
# Ubuntu apt : ffmpeg 6.1.1-3ubuntu5 (gcc 13 编译)
# Windows pip: ffmpeg>=6.1.1 (ffdl install 6.1.1 可指定版本，与 Ubuntu 对齐)
ffmpeg-downloader>=0.5.0

# Configuration
PyYAML>=6.0
